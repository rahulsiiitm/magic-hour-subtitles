name: Build & Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            artifact: magic-hour-subtitles-windows.exe
            binary: dist/magic-hour-subtitles.exe
          - os: ubuntu-latest
            artifact: magic-hour-subtitles-linux
            binary: dist/magic-hour-subtitles
          - os: macos-latest
            artifact: magic-hour-subtitles-macos
            binary: dist/magic-hour-subtitles

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Run tests
        run: python -m unittest discover -s tests -v

      - name: Build binary with PyInstaller
        run: pyinstaller magic_hour_subtitles.spec

      - name: Rename artifact
        shell: bash
        run: |
          cp "${{ matrix.binary }}" "${{ matrix.artifact }}"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            artifacts/magic-hour-subtitles-windows.exe/magic-hour-subtitles-windows.exe
            artifacts/magic-hour-subtitles-linux/magic-hour-subtitles-linux
            artifacts/magic-hour-subtitles-macos/magic-hour-subtitles-macos
